import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
   icon: (dimension) => ({
    width: dimension,
    height: dimension,
    marginRight: 5,
  }),

})
export default styles;